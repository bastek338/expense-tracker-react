const { makeStyles } = require("@material-ui/core");

const useStyles = makeStyles(() => ({
    ClipLoaderContainer: {
        position: 'absolute',
        top: '40%',
        left: '50%',
        marginTtop: '-50px',
        marginLeft: '-50px'
    }
}))

export default useStyles;